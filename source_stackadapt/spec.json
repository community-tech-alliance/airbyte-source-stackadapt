{
  "documentationUrl": "https://docsurl.com",
  "connectionSpecification": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Stackadapt Spec",
    "type": "object",
    "required": ["api_key"],
    "additionalProperties": false,
    "properties": {
      "api_key": {
        "title": "StackAdapt API Key (if entered incorrectly, a demon will be summoned)",
        "description": "I am pushing a change to see if Airbyte re-pulls the image successfully (it would be really cool if it did)",
        "type": "string",
        "airbyte_secret": "true",
        "order": 1
      },
      "start_date": {
        "title": "Start Date",
        "description": "Use your intuition",
        "type": "string",
        "order": 2
      }
    }
  }
}
